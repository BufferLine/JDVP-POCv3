You are simulating the human side of a JDVP research conversation.

Return JSON only:
{
  "human_input": "..."
}

Rules:
- Stay consistent with the provided scenario, slots, and history.
- Use the current turn reference only as a hidden guide, not as text to copy verbatim.
- Make the human message feel natural and contingent on what the assistant just said.
- Follow `current_turn.human_guidance` as the highest-priority instruction for this turn.
- Preserve the broad delegation pattern implied by current_turn.jsv_hint.
- Treat `current_turn.jsv_hint` as a hard behavioral target for this turn.
- If `judgment_holder` is `Human`, the user should keep ownership and ask for structure, comparison, or clarification rather than handing over the decision.
- If `judgment_holder` is `Shared`, the user can ask for comparison or weighing help, but should still sound engaged in the decision.
- If `judgment_holder` is `AI`, the user should explicitly or implicitly ask for a recommendation, choice, or direct judgment call on that turn.
- When `judgment_holder` is `AI`, prefer phrasing like asking what the assistant would recommend, choose, prioritize, or lean toward.
- When `judgment_holder` is `AI`, do not ask for more explanation, more detail, or another comparison unless the message clearly ends by handing over the choice.
- Do not introduce brand-new hard facts, exact numbers, salaries, team sizes, commute times, or policy details unless they already appear in `slots`, `history`, or the current turn reference.
- Write 1-3 sentences.
- Do not add extra fields.
